<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:screenOrientation="unspecified"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="chorditor" android:host="import" />
            </intent-filter>

            <!-- Supabase OAuth 콜백 딥링크 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.chorditor.app" android:host="auth-callback" />
            </intent-filter>

        </activity>

        <!-- AdMob App ID — @capacitor-community/admob 필수 등록(2026-08-30, 데일리미션/승급시험 리워드 광고) -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3016297895973220~3915837424" />

        <!-- FCM 원격 푸시 알림 아이콘: 풀컬러 런처 아이콘 fallback 방지 (흰색 실루엣 전용 모노크롬 아이콘 지정) -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_notify" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_icon_color" />

        <!-- Capacitor 기본 FCM 수신 서비스 제거 → largeIcon 붙이는 커스텀 서비스로 교체.
             MyFirebaseMessagingService가 PushNotificationsPlugin.sendRemoteMessage/onNewToken을
             그대로 호출하므로 JS 쪽 push 이벤트·토큰 등록 흐름은 동일하게 유지된다. -->
        <service
            android:name="com.capacitorjs.plugins.pushnotifications.MessagingService"
            tools:node="remove" />
        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 로컬 알림 (Android 13+ 런타임 권한) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 튜너 마이크 입력 -->
    <!-- MODIFY_AUDIO_SETTINGS 를 빼면 안 된다. Capacitor 의 BridgeWebChromeClient 는
         getUserMedia 요청을 받으면 RECORD_AUDIO 와 함께 이 권한도 launch() 하고,
         결과 Map 이 "전부 true" 일 때만 request.grant() 한다.
         선언돼 있지 않으면 이 항목이 false 로 돌아와 유저가 마이크를 허용해도
         request.deny() → getUserMedia 가 NotAllowedError 로 실패한다. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- 사진/동영상 권한 강제 제거 (플러그인이 선언해도 최종 APK에서 제거) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" tools:node="remove"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" tools:node="remove"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" tools:node="remove"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" tools:node="remove"/>

</manifest>